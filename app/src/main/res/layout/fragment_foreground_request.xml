<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/fragment_foreground_request"
    tools:context="UI.RequestPermission.ForegroundRequestFragment"
    android:background="@color/cyan">

    <!-- TODO: Update blank fragment layout -->

    <TextView
        android:id="@+id/location_question_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SO WHERE ARE YOU ?"
        android:textColor="@color/white"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.407" />

    <Button
        android:id="@+id/locationButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="FIND ME"
        android:textStyle="bold"
        app:cornerRadius="15dp"
        app:layout_constraintEnd_toEndOf="@+id/location_question_textView"
        app:layout_constraintStart_toStartOf="@+id/location_question_textView"
        app:layout_constraintTop_toBottomOf="@+id/location_question_textView" />
</androidx.constraintlayout.widget.ConstraintLayout>